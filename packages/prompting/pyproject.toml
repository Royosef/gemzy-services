[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gemzy-prompting"
version = "0.1.0"
description = "Shared prompting helpers for gemzy-server"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["prompting"]
